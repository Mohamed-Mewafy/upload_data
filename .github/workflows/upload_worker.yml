name: CimaSpace Auto Uploader

on:
  workflow_dispatch: # يتيح لك تشغيله يدوياً متى شئت بضغطة زر من جيتهب
  schedule:
    - cron: '0 */6 * * *' # يعمل تلقائياً كل 6 ساعات

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests internetarchive supabase playwright
          playwright install chromium

      - name: Run Uploader Script
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          IA_ACCESS_KEY: ${{ secrets.IA_ACCESS_KEY }}
          IA_SECRET_KEY: ${{ secrets.IA_SECRET_KEY }}
        run: |
          python main.py
